name: CI
on:
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - run: dotnet restore MonPlan.sln
      - run: dotnet build MonPlan.sln --configuration Release --no-restore
      - run: dotnet test MonPlan.sln --configuration Release --no-build
